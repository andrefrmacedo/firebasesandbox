{{ember-chart type=type data=data width=500 height=300}}

{{!if statement not needed if there is no data changes}}
{{!#if display}}
	{{!ember-chart type=type data=data width=500 height=300}}
{{!!else}}
	{{!ember-chart type=type data=data width=500 height=300}}
{{!/if}}


